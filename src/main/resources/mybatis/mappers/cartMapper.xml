<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="cartMapper">
	<insert id="cartAdd" parameterType="CartVO">
		insert into cart(cart_num,goods_num,o_num,email) values(cart_seq.nextval, #{goods_num},1,#{email})
	</insert>
  	
	<select id="cartCount" parameterType="java.util.Map" resultType="Integer">
		select count(*) from cart 
		where email=#{email} and goods_num=#{goods_num}
	</select>
</mapper>